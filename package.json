{
  "private": true,
  "workspaces": [
    "packages/*",
    "packages-dev"
  ],
  "scripts": {
    "lerna:publish": "lerna publish -m 'chore: publish'",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^13.2.1",
    "@commitlint/config-conventional": "^13.2.0",
    "@parcel/packager-ts": "^2.0.0",
    "@parcel/transformer-typescript-types": "^2.0.0",
    "husky": "^7.0.2",
    "lerna": "^4.0.0",
    "parcel": "^2.0.0",
    "typescript": "^4.4.4"
  }
}
